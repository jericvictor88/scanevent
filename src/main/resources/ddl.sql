CREATE TABLE `mydb`.`cartonEvt` (
  `label` varchar(32) NOT NULL DEFAULT '',
  `courier` varchar(32) NOT NULL DEFAULT '',
  `status` varchar(32) NOT NULL DEFAULT '',
  `date` timestamp NULL DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8